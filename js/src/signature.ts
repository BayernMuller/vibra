export class Signature {
  uri: string;
  samplems: number;

  constructor(uri: string, samplems: number) {
    this.uri = uri;
    this.samplems = samplems;
  }
}

export { Signature as Singature };
